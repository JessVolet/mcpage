import type { Metadata } from "next";
import Home from "@/app/page";

export const metadata: Metadata = {
  title: "VIOLET NODE // MC.JESSVEGA.ME",
  description: "Servidor privado de Minecraft con mods, infraestructura dedicada y alta disponibilidad.",
};

export default Home;
